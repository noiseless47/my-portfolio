/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['placehold.co'], // Add this to allow external images
  },
  // Add any other config options here
};

module.exports = nextConfig; 